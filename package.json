{
  "name": "spendifico",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-level dev tooling (Husky hooks, commitlint, lint-staged, Prettier). The backend/ and frontend/ apps keep their own package.json - this root does not manage them.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare": "husky",
    "skills": "node backend/node_modules/drizzle-kit/bin.cjs skills",
    "api:sync": "npm --prefix backend run api:spec && npm --prefix frontend run api:types"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6"
  }
}
